import { DevServerOptions } from '../../types';
import { DevServerLoggerOptions, FastifyDevServer } from '../types';
export declare function getFastifyInstance(config: DevServerOptions, logger?: DevServerLoggerOptions): FastifyDevServer;
